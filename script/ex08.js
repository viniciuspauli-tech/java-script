function operacao(){
    let a = 4;
    let b = 2;
    let diferenca;
    let dobro;
    let multi;

    diferenca = a - b;
    dobro = (a*2) + (b*3);
    multi = a * b;

    alert("usando a = 4 e b = 2:");
    alert("A diferença entre as duas variáveis: " + diferenca);
    alert("O dobro da primeira variável somado ao triplo da segunda: "+ dobro);
    alert("A multiplicação das duas variáveis: "+ multi);
}
